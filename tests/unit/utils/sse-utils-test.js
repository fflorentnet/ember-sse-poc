import sseUtils from 'sse-poc/utils/sse-utils';
import { module, test } from 'qunit';

module('Unit | Utility | sse utils');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sseUtils();
  assert.ok(result);
});
